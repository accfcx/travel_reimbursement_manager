<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxd.travel.dao.IDeptHeadDao">

    <select id="selectDeptHeadName" resultType="string">
        select hname from dept_head where hid = #{hid}
    </select>
</mapper>